import 'package:azure/azure.dart';

main() async {
  // var clientId = "42e0d***2d988c4";
  //           var secret = "Gbx2***fQpIjoae:";
  //           var tenant = "e4c9ab4***2a757fb";

  //          ClientSecretCredential credential = ClientSecretCredential(tenant, clientId, secret);
  //          string accountName = "jimtestdiag924";

  //          var url = string.Format("https://{0}.blob.core.windows.net/", accountName);
  //          var blobServiceClient = BlobServiceClient(Uri(url), credential);
  //          var foo = await blobServiceClient.GetPropertiesAsync();
  //          var containerName = "mheathtest";
  //          var containerClient = blobServiceClient.GetBlobContainerClient(containerName);
  //          await containerClient.CreateIfNotExistsAsync();
  //          // some example metadata and tags we will apply to each upload
  //          var metadata = new Dictionary<string, string> { { "UploadedBy", "Mark" }};
  //          var tags = new Dictionary<string, string> { { "HomePage", "markheath.net" }};
}
